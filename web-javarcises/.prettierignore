# Archivo generado por scripts/extraer-datos.js
data/ejercicios.js
# Generado por npm (formato propio del gestor de paquetes)
package-lock.json
# Contenido fuente (markdown generado por su propio pipeline, no se formatea)
triviales/
problemas-diseno/
# SVG (Prettier no tiene parser)
icons/
