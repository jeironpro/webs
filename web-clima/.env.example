# Variables públicas (prefijo VITE_). Opcionales: se usan los valores por
# defecto de Open-Meteo si no se definen.

# Base de la API de pronóstico
VITE_OPEN_METEO_BASE=https://api.open-meteo.com

# Base de la API de geocodificación
VITE_GEOCODING_BASE=https://geocoding-api.open-meteo.com